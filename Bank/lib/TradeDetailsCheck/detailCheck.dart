import 'package:flutter/material.dart';

class DetailCheck extends StatelessWidget {
  const DetailCheck({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('DetailCheck');
  }
}
